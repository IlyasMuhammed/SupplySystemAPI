# Copy to .env and fill in real values

# PostgreSQL connection
PGHOST=localhost
PGPORT=5432
PGDATABASE=sms_dev
PGUSER=sms_app_user
PGPASSWORD=CHANGE_ME
PGSSL=false

# For integration tests (superuser needed to create schema)
# PGUSER=postgres
# PGDATABASE=sms_test
